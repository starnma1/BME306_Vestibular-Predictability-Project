for (f in files){
frame_name <- sub("\\.xlsx$", "", basename(f))
prelim_read <- read_excel(f)
dat <- data.frame(
sample = sample,
trial = frame_name,
median_body_x = median(prelim_read$StdBodyX) # Assuming the median is in the 5th row
)
all_data <- append(all_data, list(dat))
}
}
for (participant in 1:20) {
path   <- "Data/"
sample <- paste0("B", participant)
folder <- Sys.glob(glue("{path}{sample}_*/Learning_Data"))
files <- list.files(folder, pattern = "^[^~].*\\.xlsx$", full.names = TRUE)
for (f in files){
frame_name <- sub("\\.xlsx$", "", basename(f))
prelim_read <- read_excel(f)
#dat <- data.frame(
#sample = sample,
#trial = frame_name,
#median_body_x = median(prelim_read$StdBodyX) # Assuming the median is in the 5th row
#)
all_data <- append(all_data, list(dat))
}
}
for (participant in 1:20) {
path   <- "Data/"
sample <- paste0("B", participant)
folder <- Sys.glob(glue("{path}{sample}_*/Learning_Data"))
files <- list.files(folder, pattern = "^[^~].*\\.xlsx$", full.names = TRUE)
for (f in files){
frame_name <- sub("\\.xlsx$", "", basename(f))
prelim_read <- read_excel(f)
print(head(prelim_read))
#dat <- data.frame(
#sample = sample,
#trial = frame_name,
#median_body_x = median(prelim_read$StdBodyX) # Assuming the median is in the 5th row
#)
all_data <- append(all_data, list(dat))
}
}
dat <- data.frame(
sample = sample,
trial = frame_name,
median_body_x = median(prelim_read$StdBodyX) # Assuming the median is in the 5th row
)
dat
all_data <- append(all_data, list(dat))
for (participant in 1:20) {
path   <- "Data/"
sample <- paste0("B", participant)
folder <- Sys.glob(glue("{path}{sample}_*/Learning_Data"))
files <- list.files(folder, pattern = "^[^~].*\\.xlsx$", full.names = TRUE)
for (f in files){
frame_name <- sub("\\.xlsx$", "", basename(f))
prelim_read <- read_excel(f)
print(head(prelim_read))
dat <- data.frame(
sample = sample,
trial = frame_name,
median_body_x = median(prelim_read$StdBodyX) # Assuming the median is in the 5th row
)
all_data <- append(all_data, list(dat))
}
}
for (participant in 1:20) {
path   <- "Data/"
sample <- paste0("B", participant)
folder <- Sys.glob(glue("{path}{sample}_*/Learning_Data"))
files <- list.files(folder, pattern = "^[^~].*\\.xlsx$", full.names = TRUE)
for (f in files){
frame_name <- sub("\\.xlsx$", "", basename(f))
prelim_read <- read_excel(f)
print(colnames(prelim_read))
dat <- data.frame(
sample = sample,
trial = frame_name,
median_body_x = median(prelim_read$StdBodyX) # Assuming the median is in the 5th row
)
all_data <- append(all_data, list(dat))
}
}
for (participant in 1:20) {
path   <- "Data/"
sample <- paste0("B", participant)
folder <- Sys.glob(glue("{path}{sample}_*/Learning_Data"))
files <- list.files(folder, pattern = "^[^~].*\\.xlsx$", full.names = TRUE)
for (f in files){
frame_name <- sub("\\.xlsx$", "", basename(f))
prelim_read <- read_excel(f)
print(sample, colnames(prelim_read))
dat <- data.frame(
sample = sample,
trial = frame_name,
median_body_x = median(prelim_read$StdBodyX) # Assuming the median is in the 5th row
)
all_data <- append(all_data, list(dat))
}
}
print(sample)
for (participant in 1:20) {
path   <- "Data/"
sample <- paste0("B", participant)
folder <- Sys.glob(glue("{path}{sample}_*/Learning_Data"))
files <- list.files(folder, pattern = "^[^~].*\\.xlsx$", full.names = TRUE)
for (f in files){
frame_name <- sub("\\.xlsx$", "", basename(f))
prelim_read <- read_excel(f)
print(sample)
print(colnames(prelim_read))
dat <- data.frame(
sample = sample,
trial = frame_name,
median_body_x = median(prelim_read$StdBodyX) # Assuming the median is in the 5th row
)
all_data <- append(all_data, list(dat))
}
}
library(tidyverse)
library(glue)
library(glmmTMB)
library(performance)
library(ggeffects)
library(readxl)
for (participant in 1:20) {
path   <- "Data/"
sample <- paste0("B", participant)
folder <- Sys.glob(glue("{path}{sample}_*"))
learning_dir <- file.path(folder, "Learning_Data")
if (!dir.exists(learning_dir)) dir.create(learning_dir)
files <- list.files(folder, pattern = "_trialResults\\.xlsx$", full.names = TRUE)
# Extract timestamps from filenames and sort chronologically
timestamps <- files |>
basename() |>
str_extract("\\d{4}_\\d{2}_\\d{2}_\\d{6}") |>
as.POSIXct(format = "%Y_%m_%d_%H%M%S")
ordered_files <- files[order(timestamps)]
# Copy and rename 1 to n
for (i in seq_along(ordered_files)) {
dest <- file.path(learning_dir, glue("{i}.xlsx"))
file.copy(ordered_files[i], dest, overwrite = TRUE)
}
message(glue("B{participant}: {length(ordered_files)} files processed"))
}
all_data <- list()
for (participant in 1:20) {
path   <- "Data/"
sample <- paste0("B", participant)
folder <- Sys.glob(glue("{path}{sample}_*/Learning_Data"))
files <- list.files(folder, pattern = "^[^~].*\\.xlsx$", full.names = TRUE)
for (f in files){
frame_name <- sub("\\.xlsx$", "", basename(f))
prelim_read <- read_excel(f)
print(sample)
print(colnames(prelim_read))
dat <- data.frame(
sample = sample,
trial = frame_name,
median_body_x = median(prelim_read$StdBodyX) # Assuming the median is in the 5th row
)
all_data <- append(all_data, list(dat))
}
}
# Script to make a dataset for learning effect analysis #
###############################################################################
library(tidyverse)
library(glue)
library(glmmTMB)
library(performance)
library(ggeffects)
library(readxl)
###############################################################################
# Read all the data and create new files labbeled 1-6 based on
# when they were done, aka the timestamp.
for (participant in 1:20) {
path   <- "Data/"
sample <- paste0("B", participant)
folder <- Sys.glob(glue("{path}{sample}_*"))
learning_dir <- file.path(folder, "Learning_Data")
if (!dir.exists(learning_dir)) dir.create(learning_dir)
files <- list.files(folder, pattern = "_trialResults\\.xlsx$", full.names = TRUE)
# Extract timestamps from filenames and sort chronologically
timestamps <- files |>
basename() |>
str_extract("\\d{4}_\\d{2}_\\d{2}_\\d{6}") |>
as.POSIXct(format = "%Y_%m_%d_%H%M%S")
ordered_files <- files[order(timestamps)]
# Copy and rename 1 to n
for (i in seq_along(ordered_files)) {
dest <- file.path(learning_dir, glue("{i}.xlsx"))
file.copy(ordered_files[i], dest, overwrite = TRUE)
}
message(glue("B{participant}: {length(ordered_files)} files processed"))
}
###############################################################################
# Compile all those files into one dataset, using only the median from each trial
all_data <- list()
for (participant in 1:20) {
if (participant == 11) next
path   <- "Data/"
sample <- paste0("B", participant)
folder <- Sys.glob(glue("{path}{sample}_*/Learning_Data"))
files <- list.files(folder, pattern = "^[^~].*\\.xlsx$", full.names = TRUE)
for (f in files){
frame_name <- sub("\\.xlsx$", "", basename(f))
prelim_read <- read_excel(f)
print(sample)
print(colnames(prelim_read))
dat <- data.frame(
sample = sample,
trial = frame_name,
median_body_x = median(prelim_read$StdBodyX) # Assuming the median is in the 5th row
)
all_data <- append(all_data, list(dat))
}
}
all_data
final_data <- bind_rows(all_data)
final_data
write.csv("Data_for_stats/learning_curve_data.csv", final_data, row.names = FALSE)")
# Script to make a dataset for learning effect analysis #
###############################################################################
library(tidyverse)
library(glue)
library(glmmTMB)
library(performance)
library(ggeffects)
library(readxl)
###############################################################################
# Read all the data and create new files labbeled 1-6 based on
# when they were done, aka the timestamp.
for (participant in 1:20) {
path   <- "Data/"
# Script to make a dataset for learning effect analysis #
###############################################################################
library(tidyverse)
library(glue)
library(glmmTMB)
library(performance)
library(ggeffects)
library(readxl)
###############################################################################
# Read all the data and create new files labbeled 1-6 based on
# when they were done, aka the timestamp.
for (participant in 1:20) {
path   <- "Data/"
sample <- paste0("B", participant)
folder <- Sys.glob(glue("{path}{sample}_*"))
learning_dir <- file.path(folder, "Learning_Data")
if (!dir.exists(learning_dir)) dir.create(learning_dir)
files <- list.files(folder, pattern = "_trialResults\\.xlsx$", full.names = TRUE)
# Extract timestamps from filenames and sort chronologically
timestamps <- files |>
basename() |>
str_extract("\\d{4}_\\d{2}_\\d{2}_\\d{6}") |>
as.POSIXct(format = "%Y_%m_%d_%H%M%S")
ordered_files <- files[order(timestamps)]
# Copy and rename 1 to n
for (i in seq_along(ordered_files)) {
dest <- file.path(learning_dir, glue("{i}.xlsx"))
file.copy(ordered_files[i], dest, overwrite = TRUE)
}
message(glue("B{participant}: {length(ordered_files)} files processed"))
}
###############################################################################
# Compile all those files into one dataset, using only the median from each trial
all_data <- list()
for (participant in 1:20) {
if (participant == 11) next
path   <- "Data/"
sample <- paste0("B", participant)
folder <- Sys.glob(glue("{path}{sample}_*/Learning_Data"))
files <- list.files(folder, pattern = "^[^~].*\\.xlsx$", full.names = TRUE)
for (f in files){
frame_name <- sub("\\.xlsx$", "", basename(f))
prelim_read <- read_excel(f)
print(sample)
print(colnames(prelim_read))
dat <- data.frame(
sample = sample,
trial = frame_name,
median_body_x = median(prelim_read$StdBodyX),
direction = prelim_read$Direction
)
all_data <- append(all_data, list(dat))
}
}
final_data <- bind_rows(all_data)
# Data set complete
write.csv("Data_for_stats/learning_curve_data.csv", final_data, row.names = FALSE)")
learning_curve_data <- bind_rows(all_data)
# Data set complete
write.csv("Data_for_stats/learning_curve_data.csv", learning_curve_data, row.names = FALSE)")
# Script to make a dataset for learning effect analysis #
###############################################################################
library(tidyverse)
library(glue)
library(glmmTMB)
library(performance)
library(ggeffects)
library(readxl)
###############################################################################
# Read all the data and create new files labbeled 1-6 based on
# when they were done, aka the timestamp.
for (participant in 1:20) {
path   <- "Data/"
sample <- paste0("B", participant)
folder <- Sys.glob(glue("{path}{sample}_*"))
learning_dir <- file.path(folder, "Learning_Data")
if (!dir.exists(learning_dir)) dir.create(learning_dir)
files <- list.files(folder, pattern = "_trialResults\\.xlsx$", full.names = TRUE)
# Extract timestamps from filenames and sort chronologically
timestamps <- files |>
basename() |>
str_extract("\\d{4}_\\d{2}_\\d{2}_\\d{6}") |>
as.POSIXct(format = "%Y_%m_%d_%H%M%S")
ordered_files <- files[order(timestamps)]
# Copy and rename 1 to n
for (i in seq_along(ordered_files)) {
dest <- file.path(learning_dir, glue("{i}.xlsx"))
file.copy(ordered_files[i], dest, overwrite = TRUE)
}
message(glue("B{participant}: {length(ordered_files)} files processed"))
}
###############################################################################
# Compile all those files into one dataset, using only the median from each trial
all_data <- list()
for (participant in 1:20) {
if (participant == 11) next
path   <- "Data/"
sample <- paste0("B", participant)
folder <- Sys.glob(glue("{path}{sample}_*/Learning_Data"))
files <- list.files(folder, pattern = "^[^~].*\\.xlsx$", full.names = TRUE)
for (f in files){
frame_name <- sub("\\.xlsx$", "", basename(f))
prelim_read <- read_excel(f)
print(sample)
print(colnames(prelim_read))
dat <- data.frame(
sample = sample,
trial = frame_name,
median_body_x = median(prelim_read$StdBodyX),
direction = prelim_read$Direction
)
all_data <- append(all_data, list(dat))
}
}
learning_curve_data <- bind_rows(all_data)
# Data set complete
write.csv("Data_for_stats/learning_curve_data.csv", learning_curve_data, row.names = FALSE)")
# Data set complete
write.csv("Data_for_stats/learning_curve_data.csv", learning_curve_data, row.names = FALSE))
# Data set complete
write.csv("Data_for_stats/learning_curve_data.csv", learning_curve_data, row.names = FALSE)
# Data set complete
write.csv(learning_curve_data, "Data_for_stats/learning_curve_data.csv", row.names = FALSE)
dat <- prelim_read %>%
group_by(Direction)
dat
dat <- prelim_read %>%
group_by(Direction)
dat <- prelim_read %>%
group_by(Direction) %>%
summarise(median_body_x = median(StdBodyX))
dat
final_dat <- data.frame(
sample = sample,
trial = frame_name,
median_body_x = dat$median_body_x,
direction = dat$Direction
)
for (participant in 1:20) {
if (participant == 11) next
path   <- "Data/"
sample <- paste0("B", participant)
folder <- Sys.glob(glue("{path}{sample}_*/Learning_Data"))
files <- list.files(folder, pattern = "^[^~].*\\.xlsx$", full.names = TRUE)
for (f in files){
frame_name <- sub("\\.xlsx$", "", basename(f))
prelim_read <- read_excel(f)
print(sample)
print(colnames(prelim_read))
dat <- prelim_read %>%
group_by(Direction) %>%
summarise(median_body_x = median(StdBodyX))
final_dat <- data.frame(
sample = sample,
trial = frame_name,
median_body_x = dat$median_body_x,
direction = dat$Direction
)
all_data <- append(all_data, list(dat))
}
}
learning_curve_data <- bind_rows(all_data)
# Data set complete
write.csv(learning_curve_data, "Data_for_stats/learning_curve_data.csv", row.names = FALSE)
all_data
final_dat
for (participant in 1:20) {
if (participant == 11) next
path   <- "Data/"
sample <- paste0("B", participant)
folder <- Sys.glob(glue("{path}{sample}_*/Learning_Data"))
files <- list.files(folder, pattern = "^[^~].*\\.xlsx$", full.names = TRUE)
for (f in files){
frame_name <- sub("\\.xlsx$", "", basename(f))
prelim_read <- read_excel(f)
print(sample)
print(colnames(prelim_read))
dat <- prelim_read %>%
group_by(Direction) %>%
summarise(median_body_x = median(StdBodyX))
final_dat <- data.frame(
sample = sample,
trial = frame_name,
median_body_x = dat$median_body_x,
direction = dat$Direction
)
all_data <- append(all_data, list(final_dat))
}
}
learning_curve_data <- bind_rows(all_data)
# Data set complete
write.csv(learning_curve_data, "Data_for_stats/learning_curve_data.csv", row.names = FALSE)
learning_curve_data
final_dat
learning_curve_data
for (participant in 1:20) {
if (participant == 11) next
path   <- "Data/"
sample <- paste0("B", participant)
folder <- Sys.glob(glue("{path}{sample}_*/Learning_Data"))
files <- list.files(folder, pattern = "^[^~].*\\.xlsx$", full.names = TRUE)
for (f in files){
frame_name <- sub("\\.xlsx$", "", basename(f))
prelim_read <- read_excel(f)
print(sample)
print(colnames(prelim_read))
dat <- prelim_read %>%
group_by(Direction) %>%
summarise(median_body_x = median(StdBodyX))
final_dat <- data.frame(
sample = sample,
trial = frame_name,
median_body_x = dat$median_body_x,
direction = dat$Direction
)
print(head(final_dat))
all_data <- append(all_data, list(final_dat))
}
}
all_data <- c(all_data, list(final_dat))
all_data
learning_curve_data <- bind_rows(all_data)
learning_curve_data
for (participant in 1:20) {
if (participant == 11) next
path   <- "Data/"
sample <- paste0("B", participant)
folder <- Sys.glob(glue("{path}{sample}_*/Learning_Data"))
files <- list.files(folder, pattern = "^[^~].*\\.xlsx$", full.names = TRUE)
for (f in files){
frame_name <- sub("\\.xlsx$", "", basename(f))
prelim_read <- read_excel(f)
print(sample)
print(colnames(prelim_read))
dat <- prelim_read %>%
group_by(Direction) %>%
summarise(median_body_x = median(StdBodyX))
final_dat <- data.frame(
sample = sample,
trial = frame_name,
median_body_x = dat$median_body_x,
direction = dat$Direction
)
all_data <- c(all_data, list(final_dat))
}
}
learning_curve_data <- bind_rows(all_data)
learning_curve_data
all_data <- list()
for (participant in 1:20) {
if (participant == 11) next
path   <- "Data/"
sample <- paste0("B", participant)
folder <- Sys.glob(glue("{path}{sample}_*/Learning_Data"))
files <- list.files(folder, pattern = "^[^~].*\\.xlsx$", full.names = TRUE)
for (f in files){
frame_name <- sub("\\.xlsx$", "", basename(f))
prelim_read <- read_excel(f)
print(sample)
print(colnames(prelim_read))
dat <- prelim_read %>%
group_by(Direction) %>%
summarise(median_body_x = median(StdBodyX))
final_dat <- data.frame(
sample = sample,
trial = frame_name,
median_body_x = dat$median_body_x,
direction = dat$Direction
)
all_data <- c(all_data, list(final_dat))
}
}
learning_curve_data <- bind_rows(all_data)
learning_curve_data
# Data set complete
write.csv(learning_curve_data, "Data_for_stats/learning_curve_data.csv", row.names = FALSE)
